name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ${{ matrix.os }}-latest

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu, macos]

    steps:
      - uses: actions/checkout@v2

      - name: Set up Ruby
        uses: tenderlove/setup-ruby@b3d7dd38e59eb5a0fda3b203bda1735ba3347cd0
        with:
          ruby-version: "3.0.2"
      - name: Install dependencies
        run: bundle install
      - name: Run tests
        run: bundle exec rake test
